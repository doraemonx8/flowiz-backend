# Quota Engine - Architecture & Flow Diagrams

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          CLIENT / FRONTEND                              │
│                    (Web App / Mobile / API Client)                      │
└────────────────────────────────┬────────────────────────────────────────┘
                                 │
                    ┌────────────▼────────────┐
                    │  Express Request/Auth   │
                    │   (JWT Middleware)      │
                    └────────────┬────────────┘
                                 │
        ┌────────────────────────▼────────────────────────────┐
        │                                                     │
        │         QUOTA CHECKING LAYER                       │
        │  ┌──────────────────────────────────────────────┐  │
        │  │ checkQuota('email_messages')                 │  │
        │  │ checkMultipleQuotas(['email', 'storage'])    │  │
        │  │ Middleware: Enforce quota per route          │  │
        │  └──────────────┬───────────────────────────────┘  │
        │                 │                                  │
        └─────────────────┼──────────────────────────────────┘
                          │
        ┌─────────────────▼──────────────────────┐
        │  CONTROLLERS / SERVICE LAYER           │
        │ ┌──────────────────────────────────┐  │
        │ │ EmailController.sendEmail()      │  │
        │ │ CampaignController.create()      │  │
        │ │ LeadController.import()          │  │
        │ │ QuotaController.dashboard()      │  │
        │ └──────────────┬───────────────────┘  │
        └─────────────────┼─────────────────────┘
                          │
        ┌─────────────────▼──────────────────────────────────┐
        │       INTEGRATION SERVICES                         │
        │ ┌────────────────────────────────────────────────┐ │
        │ │ EmailQuotaService                              │ │
        │ │ CampaignQuotaService                           │ │
        │ │ LeadQuotaService                               │ │
        │ │ GenericQuotaService.executeWithQuotaCheck()    │ │
        │ └────────────────────────────────────────────────┘ │
        └─────────────────┬────────────────────────────────┬─┘
                          │                                │
        ┌─────────────────▼────────────────────────────────▼─┐
        │          QUOTA ENGINE (Core Logic)                 │
        │ ┌──────────────────────────────────────────────┐   │
        │ │ checkQuota()                                 │   │
        │ │ deductUsage()                                │   │
        │ │ refundUsage()                                │   │
        │ │ adjustQuota()                                │   │
        │ │ getUsageStats()                              │   │
        │ │ initializePlanQuota()                        │   │
        │ │ addAddonQuota()                              │   │
        │ │ getCurrentBalance()                          │   │
        │ └──────────────────────────────────────────────┘   │
        └────────────────────┬────────────────────────────────┘
                             │
        ┌────────────────────▼───────────────────────────────┐
        │           DATABASE LAYER                           │
        │                                                    │
        │  ┌──────────────┐  ┌──────────────────┐           │
        │  │ features     │  │ plans            │           │
        │  │ ─ id         │  │ ─ id             │           │
        │  │ ─ slug       │  │ ─ name           │           │
        │  │ ─ name       │  │ ─ lemon_id       │           │
        │  │ ─ status     │  │ ─ status         │           │
        │  └──────┬───────┘  └──────┬───────────┘           │
        │         │                 │                       │
        │         └────────┬────────┘                        │
        │                  │                                 │
        │       ┌──────────▼──────────┐                     │
        │       │ plan_features       │                     │
        │       │ ─ plan_id           │                     │
        │       │ ─ feature_id        │                     │
        │       │ ─ limit_value       │                     │
        │       └──────────┬──────────┘                     │
        │                  │                                 │
        │  ┌───────────────┼──────────────────┐             │
        │  │               │                  │             │
        │  ▼               ▼                  ▼             │
        │┌────────────┐  ┌────────────┐  ┌────────────┐   │
        ││subscriptions││ addons      ││ ledger      │   │
        ││─ id         ││ ─ id        ││ ─ id        │   │
        ││─ user_id    ││ ─ name      ││ ─ user_id   │   │
        ││─ plan_id    ││ ─ feature_id││ ─ feature_id│   │
        ││─ status     ││ ─ unit_amt  ││ ─ amount    │   │
        ││─ period_*   ││ ─ status    ││ ─ is_deposit│   │
        │└────────────┘└────────────┘└────────────┘│   │
        │                                          │   │
        │      ┌──────────────────────────┐       │   │
        │      │ subscription_addons      │       │   │
        │      │ ─ subscription_id        │       │   │
        │      │ ─ addon_id               │       │   │
        │      │ ─ quantity               │       │   │
        │      └──────────────────────────┘       │   │
        └──────────────────────────────────────────┘
```

---

## 📊 Request Flow Diagram

```
USER REQUEST
    │
    ▼
┌─────────────────────────┐
│ Route Handler Receives  │
│ POST /send-email        │
└────────┬────────────────┘
         │
         ▼
    ┌────────────────────────────────────────┐
    │ checkQuota Middleware                  │
    │ (Attached to route)                    │
    └────────┬───────────────────────────────┘
             │
             ▼
    ┌────────────────────────────────────┐
    │ QuotaEngine.checkQuota(userId,    │
    │ 'email_messages')                  │
    └────────┬───────────────────────────┘
             │
             ├─► Query subscriptions (active, period valid)
             │
             ├─► Get plan_features (limit_value)
             │
             ├─► Get addon quota (subscription_addons)
             │
             └─► Sum ledger balance
                  │
                  ▼
    ┌────────────────────────────────────┐
    │ Calculate:                         │
    │ - Total limit = plan + addons      │
    │ - Total used = ledger sum          │
    │ - Remaining = limit - used         │
    └────────┬───────────────────────────┘
             │
             ▼
    ┌────────────────────────────────────┐
    │ Quota Available?                   │
    └─┬──────────────────────────────┬──┘
      │ YES                         │ NO
      │                              │
      ▼                              ▼
┌──────────────────┐         ┌─────────────────┐
│ Continue to      │         │ Return 429      │
│ Controller       │         │ Quota Exhausted │
│                  │         │ + Info          │
└────────┬─────────┘         └─────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│ Execute Business Logic           │
│ (e.g., send email)               │
└────────┬───────────────────────┬─┘
         │                       │
      SUCCESS                 FAILURE
         │                       │
         ▼                       ▼
    ┌─────────────────────────────────────┐
    │ Call safeDeductUsage()              │
    │ QuotaEngine.deductUsage({           │
    │   userId, featureSlug, amount: 1,   │
    │   source: 'consumption',            │
    │   description: 'Email sent'         │
    │ })                                  │
    └────────┬──────────────────────────┘
             │
             ├─► Get feature_id from slug
             │
             ├─► Start DB transaction
             │
             ├─► Verify feature exists & active
             │
             ├─► Get current balance
             │
             ├─► Check: balance >= amount
             │
             ├─► Calculate new balance
             │
             ├─► INSERT ledger entry
             │    {
             │      user_id, feature_id, amount,
             │      is_deposit: 0, source: 'consumption',
             │      balance_after: newBalance
             │    }
             │
             ├─► COMMIT transaction
             │
             ▼
    ┌────────────────────────────┐
    │ Return Success Response    │
    │ {                          │
    │   status: true,            │
    │   data: {                  │
    │     success: true,         │
    │     balance: 499,          │
    │     ledgerId: 5678         │
    │   }                        │
    │ }                          │
    └────────────────────────────┘
```

---

## 🔄 Usage Deduction Flow

```
┌─────────────────────────────────────────────────────────────────┐
│ safeDeductUsage(userId, featureSlug, amount, description)       │
└────────┬────────────────────────────────────────────────────────┘
         │
         ▼
    ┌──────────────────────────────────┐
    │ Get current balance for          │
    │ user_id + feature_id             │
    └────────┬───────────────────────┬─┘
             │                       │
        FOUND                    NOT FOUND
         │                       │
         ▼                       ▼
    ┌─────────────┐      ┌─────────────┐
    │ Current:    │      │ Current:    │
    │ $balance =  │      │ $balance =  │
    │ 500         │      │ 0           │
    └────────┬────┘      └────────┬────┘
             │                    │
             └────────┬───────────┘
                      │
                      ▼
        ┌────────────────────────────┐
        │ Check if sufficient        │
        │ balance >= amount?         │
        └─┬──────────────────────┬──┘
          │ YES                  │ NO
          │                       │
          ▼                       ▼
    ┌──────────────┐      ┌─────────────────┐
    │ Calculate    │      │ Return error:   │
    │ new balance  │      │ "Insufficient   │
    │ = balance -  │      │ quota"          │
    │   amount     │      │                 │
    │ = 500 - 1    │      │ balance: 0      │
    │ = 499        │      └─────────────────┘
    └────────┬─────┘
             │
             ▼
    ┌──────────────────────────┐
    │ INSERT INTO ledger:      │
    │ {                        │
    │   user_id: 123,          │
    │   feature_id: 5,         │
    │   amount: 1,             │
    │   is_deposit: 0,         │
    │   source: 'consumption', │
    │   balance_after: 499,    │
    │   description: '...',    │
    │   created_on: NOW(),     │
    │   is_deleted: '0'        │
    │ }                        │
    └────────┬─────────────────┘
             │
             ▼
    ┌────────────────────────┐
    │ COMMIT transaction     │
    └────────┬───────────────┘
             │
             ▼
    ┌────────────────────────────────┐
    │ Return success response:       │
    │ {                              │
    │   success: true,               │
    │   balance: 499,                │
    │   ledgerId: 12345,             │
    │   message: 'Usage deducted'    │
    │ }                              │
    └────────────────────────────────┘
```

---

## 🎯 Query Flow for checkQuota()

```
┌─────────────────────────────────────────────────────────────────┐
│ QuotaEngine.checkQuota(userId: 123, featureSlug: 'email_messages')
└────────┬────────────────────────────────────────────────────────┘
         │
         ▼
    ┌─────────────────────────────────────────┐
    │ Query 1: Get Active Subscription        │
    │                                         │
    │ SELECT s.id, pf.limit_value             │
    │ FROM subscriptions s                    │
    │ INNER JOIN plans p ON p.id = s.plan_id  │
    │ INNER JOIN plan_features pf ON pf.plan_id = p.id
    │ INNER JOIN features f ON f.id = pf.feature_id
    │ WHERE s.user_id = 123                   │
    │   AND f.slug = 'email_messages'         │
    │   AND s.status IN ('active', 'on_trial')
    │   AND s.current_period_end > NOW()      │
    │ LIMIT 1                                 │
    │                                         │
    │ Result: {                               │
    │   subscriptionId: 456,                  │
    │   planLimit: 1000                       │
    │ }                                       │
    └────────┬────────────────────────────────┘
             │
             ▼
    ┌─────────────────────────────────────────┐
    │ Query 2: Get Addon Quota                │
    │                                         │
    │ SELECT SUM(a.unit_amount * sa.quantity) │
    │ FROM subscription_addons sa             │
    │ INNER JOIN addons a ON a.id = sa.addon_id
    │ WHERE sa.subscription_id = 456          │
    │   AND a.feature_id = (SELECT id FROM   │
    │        features WHERE slug = 'email_messages')
    │                                         │
    │ Result: { totalAddonQuota: 500 }        │
    └────────┬────────────────────────────────┘
             │
             ▼
    ┌─────────────────────────────────────────┐
    │ Query 3: Get Current Usage              │
    │                                         │
    │ SELECT                                  │
    │   SUM(CASE WHEN is_deposit = 0          │
    │        THEN amount ELSE 0 END) as used, │
    │   SUM(CASE WHEN is_deposit = 1          │
    │        THEN amount ELSE 0 END) as deposited
    │ FROM ledger                             │
    │ WHERE user_id = 123                     │
    │   AND feature_id = (SELECT id FROM     │
    │        features WHERE slug = 'email_messages')
    │                                         │
    │ Result: {                               │
    │   used: 300,                            │
    │   deposited: 1500                       │
    │ }                                       │
    └────────┬────────────────────────────────┘
             │
             ▼
    ┌─────────────────────────────────────────┐
    │ Calculate Final Values:                 │
    │                                         │
    │ totalLimit = planLimit + addonQuota     │
    │            = 1000 + 500 = 1500          │
    │                                         │
    │ remaining = totalLimit - used           │
    │          = 1500 - 300 = 1200            │
    │                                         │
    │ allowed = remaining > 0                 │
    │         = 1200 > 0 = true               │
    └────────┬────────────────────────────────┘
             │
             ▼
    ┌─────────────────────────────────────────┐
    │ Return Result:                          │
    │ {                                       │
    │   allowed: true,                        │
    │   remaining: 1200,                      │
    │   used: 300,                            │
    │   limit: 1500,                          │
    │   subscriptionId: 456,                  │
    │   status: 'valid'                       │
    │ }                                       │
    └─────────────────────────────────────────┘
```

---

## 🗂️ Database State After Operations

```
INITIAL STATE
─────────────

subscriptions:
┌────┬─────────┬──────────┬────────┐
│ id │ user_id │ plan_id  │ status │
├────┼─────────┼──────────┼────────┤
│ 1  │ 123     │ 2 (Pro)  │ active │
└────┴─────────┴──────────┴────────┘

plan_features:
┌────┬─────────┬────────────┬─────────────┐
│ id │ plan_id │ feature_id │ limit_value │
├────┼─────────┼────────────┼─────────────┤
│ 1  │ 2       │ 1 (email)  │ 50000       │
└────┴─────────┴────────────┴─────────────┘

ledger (empty initially):
┌────┬─────────┬────────────┬────────┬──────────┐
│ id │ user_id │ feature_id │ amount │ balance  │
├────┼─────────┼────────────┼────────┼──────────┤
└────┴─────────┴────────────┴────────┴──────────┘

AFTER checkQuota (first call):
────────────────────────────────────
→ Creates initial ledger entry when subscription starts
ledger:
┌────┬─────────┬────────────┬────────┬───────────┐
│ id │ user_id │ feature_id │ amount │ balance   │
├────┼─────────┼────────────┼────────┼───────────┤
│ 1  │ 123     │ 1          │ 50000  │ 50000     │ (deposit)
└────┴─────────┴────────────┴────────┴───────────┘

AFTER sendEmail (deducts 1):
──────────────────────────────
ledger:
┌────┬─────────┬────────────┬────────┬───────────┐
│ id │ user_id │ feature_id │ amount │ balance   │
├────┼─────────┼────────────┼────────┼───────────┤
│ 1  │ 123     │ 1          │ 50000  │ 50000     │ (deposit)
│ 2  │ 123     │ 1          │ 1      │ 49999     │ (consumption)
└────┴─────────┴────────────┴────────┴───────────┘

AFTER sendBatch (deducts 100):
────────────────────────────────
ledger:
┌────┬─────────┬────────────┬────────┬───────────┐
│ id │ user_id │ feature_id │ amount │ balance   │
├────┼─────────┼────────────┼────────┼───────────┤
│ 1  │ 123     │ 1          │ 50000  │ 50000     │ (deposit)
│ 2  │ 123     │ 1          │ 1      │ 49999     │ (consumption)
│ 3  │ 123     │ 1          │ 100    │ 49899     │ (consumption)
└────┴─────────┴────────────┴────────┴───────────┘

AFTER refund (adds back 50):
──────────────────────────────
ledger:
┌────┬─────────┬────────────┬────────┬───────────┐
│ id │ user_id │ feature_id │ amount │ balance   │
├────┼─────────┼────────────┼────────┼───────────┤
│ 1  │ 123     │ 1          │ 50000  │ 50000     │ (deposit)
│ 2  │ 123     │ 1          │ 1      │ 49999     │ (consumption)
│ 3  │ 123     │ 1          │ 100    │ 49899     │ (consumption)
│ 4  │ 123     │ 1          │ 50     │ 49949     │ (refund)
└────┴─────────┴────────────┴────────┴───────────┘

CURRENT STATE:
──────────────
Available quota: 49949 / 50000 (99.9%)
Used: 101
```

---

## 📈 Real-World Example Flow

```
USER SENDS 100 EMAILS
│
├─ checkQuota('email_messages')
│  └─ Returns: { allowed: true, remaining: 49949, limit: 50000 }
│
├─ sendBatchEmails([100 emails])
│  ├─ Email 1 sent ✓
│  ├─ Email 2 sent ✓
│  ├─ ...
│  ├─ Email 100 sent ✓
│  └─ Returns: 100 successful
│
├─ deductUsage({
│    userId: 123,
│    featureSlug: 'email_messages',
│    amount: 100,
│    source: 'consumption',
│    description: 'Batch sent: 100/100'
│  })
│  └─ Creates ledger entry: amount=100, balance_after=49899
│
└─ RESPONSE
   {
     status: true,
     message: '100/100 emails sent',
     remainingQuota: 49899,
     usedToday: 101
   }

NEXT REQUEST (checkQuota again):
────────────────────────────────
└─ Returns: { allowed: true, remaining: 49899, limit: 50000 }
   (Ledger now shows balance_after: 49899 as current balance)
```

---

## 🔐 Transaction Safety

```
deductUsage() Operation:
────────────────────────

START TRANSACTION
│
├─ BEGIN
│  ├─ SELECT feature_id FROM features WHERE slug = ?
│  ├─ SELECT balance_after FROM ledger ... ORDER BY created_on DESC LIMIT 1
│  ├─ Verify balance >= amount (for consumption)
│  ├─ Calculate newBalance = oldBalance - amount
│  ├─ INSERT INTO ledger (user_id, feature_id, amount, balance_after, ...)
│  └─ COMMIT
│
└─ TRANSACTION ENDS

If any step fails:
├─ ROLLBACK (all changes reverted)
├─ No partial updates in database
├─ Application can retry safely
└─ Data integrity preserved
```

---

## 📊 Example Dashboard Response

```
GET /api/quota/dashboard

Response:
{
  "status": true,
  "data": [
    {
      "id": 1,
      "name": "Email Messages",
      "slug": "email_messages",
      "planLimit": 50000,
      "used": 101,
      "remaining": 49899,
      "addonQuota": 5000,
      "subscriptionStatus": "active",
      "periodEnd": "2025-02-07T00:00:00Z"
    },
    {
      "id": 2,
      "name": "Campaigns",
      "slug": "campaigns",
      "planLimit": 50,
      "used": 3,
      "remaining": 47,
      "addonQuota": 0,
      "subscriptionStatus": "active",
      "periodEnd": "2025-02-07T00:00:00Z"
    },
    {
      "id": 3,
      "name": "Storage (GB)",
      "slug": "storage_gb",
      "planLimit": 10,
      "used": 2.5,
      "remaining": 7.5,
      "addonQuota": 5,
      "subscriptionStatus": "active",
      "periodEnd": "2025-02-07T00:00:00Z"
    }
  ]
}
```

---

## 🎯 Integration Point Example

```
CONTROLLER: emailController.sendBatchEmails()
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│ 1. Check Quota                                             │
│    quota = await QuotaEngine.checkQuota(userId, 'email')  │
│                                                             │
│ 2. Early Exit if No Quota                                 │
│    if (!quota.allowed) return error(429)                  │
│                                                             │
│ 3. Execute Operation                                       │
│    results = await sendEmailsToProvider(emails)           │
│                                                             │
│ 4. Count Successes                                         │
│    successCount = results.filter(r => r.ok).length        │
│                                                             │
│ 5. Deduct Usage                                            │
│    await safeDeductUsage(                                 │
│      userId,                                               │
│      'email_messages',                                     │
│      successCount,                                          │
│      `Sent ${successCount} emails`                         │
│    )                                                        │
│                                                             │
│ 6. Return Response                                          │
│    return {                                                │
│      sent: successCount,                                   │
│      remaining: quota.remaining - successCount,           │
│      quotaRefreshDate: nextBillingDate                     │
│    }                                                        │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**All diagrams created! You now have a complete visual understanding of the system. 🚀**
